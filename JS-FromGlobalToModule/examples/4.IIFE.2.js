(function ($) {

    var init, initSlider, initModal, initSearch;

    init = function () {
        initSlider();
        initModal();
        initSearch();
    };

    initSlider = function () {

    };

    initModal = function () {

    };

    initSearch = function () {

    };

    $(document).ready(function () {
        init();
    });

}(jQuery));